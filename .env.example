# Local Debug builds only. Copy this file to .env and fill in the keys.
# .env is ignored by Git and is never bundled into the app.
# Copy it to the phone's Documents/dev.env after installing (see README).

# Optional: improves the final transcript; Apple Speech shows live words.
OPENAI_API_KEY=
OPENAI_TRANSCRIPTION_MODEL=gpt-transcribe
# Interprets the request (destination / area / yes / no / choose). GPT-5.5 with reasoning off
# answers in about a second; set OPENAI_INTENT_REASONING=low for harder cases at ~1.5 s.
OPENAI_INTENT_MODEL=gpt-5.5
OPENAI_INTENT_REASONING=none

# Optional: MBTA V3 API key (free at https://api-v3.mbta.com/register). Without it the
# limit is 20 requests/min, enough for one phone; with it 1000/min.
MBTA_API_KEY=

# Deepgram (https://console.deepgram.com, $200 free credit) speaks the replies (Flux TTS) and,
# when set, supplies the final transcript (Nova-3 with Boston station and chain names as
# keyterms). Without a key, OpenAI above refines the transcript and the iPhone voice speaks.
DEEPGRAM_API_KEY=
# Flux voice: flux-hannah-en, flux-miles-en (calm male), flux-cliff-en, flux-colin-en, ...
DEEPGRAM_VOICE=flux-hannah-en
# -2 (calm) to 2 (animated)
DEEPGRAM_EXPRESSIVITY=0
# Delivery speed for spoken replies, any voice (0.7–1.2).
DEEPGRAM_VOICE_SPEED=1.2
